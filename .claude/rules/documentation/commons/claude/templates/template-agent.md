---
name: {agent-name}
description: >
  {このエージェントが何をするか。どのような条件で呼び出されるか}
tools: {使用ツール}
model: {sonnet / opus / haiku}
---
{役割宣言: このエージェントの専門領域と、関心外の領域を明示する}

## 必須参照ルール

レビュー開始前に以下を読み込むこと:

- `.claude/rules/{ルールファイルパス}`

## 手順

1. {ステップ1}
2. {ステップ2}

## 出力フォーマット

```markdown
## {agent-name} 結果

{出力内容}
```
